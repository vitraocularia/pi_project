#include <stdio.h>
#include "helpers.h"

void reverseTable(float [], size_t, size_t);

size_t extractPartOfATable(float [], size_t, size_t);

void quicksort(float [], size_t, size_t);

void volDecrease(float [], size_t, size_t);
void volIncrease(float [], size_t, size_t);